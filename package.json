{
  "name": "deonmenezes-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build:resources": "node scripts/build-resource-pages.js",
    "vercel-build": "pnpm run build:resources",
    "test": "pnpm run build:resources && node --test test/*.test.js",
    "migrate": "node scripts/migrate.js",
    "seed:social": "node scripts/seed-social.js",
    "check": "node scripts/check-social.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "@vercel/functions": "3.7.5"
  }
}
